## ITCA Backend

---

#### Dependencies:
- Flask (API Framework)
- Psycopg (Postgres integration)
- UV (Python package manager)

---

#### Installation:

- `pip install uv` (If you don't have it installed on your PC yet)
- `uv sync` (To install dependencies)

---

#### Running

- `uv run py -m src`